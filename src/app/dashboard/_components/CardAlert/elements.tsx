import { styled } from '@mui/material/styles';
import Card from '@mui/material/Card';

export const AlertCard = styled(Card)(({ theme }) => ({
  margin: theme.spacing(1.5),
  padding: theme.spacing(1.5),
}));
